/**
* name: Chris
* date 2022-04-29
* description: grenade class that extends to item. Smoke Grenade that blocks vision
*/

public class Smoke extends Grenade {

  private int enemiesSmoked;

  public Smoke (double cost, String name, int killAward, int damage) {
    super(cost, name, killAward, damage);
    enemiesSmoked = 0;
  }
  
  public Smoke (double cost, String name) {
    super(cost, name, killAward, damage);
    enemiesSmoked = 0;
  }
  
    public int getKillAward() {
    return this.killAward;
  }
  
    public int getDamage() {
    return this.damage;
  }

    public int getEnemiesSmoked() {
    return this.enemiesSmoked;
  }

  public void smoke() {
    get.enemySkill = enemySki
    System.out.println("ssssssssssssssssss");
  }
  
  public String toString() {
		return "\nenemiesSmoked: " + enemiesSmoked + "\ndamage: " + damage + "\nkillAward" + killAward;
	}
}
